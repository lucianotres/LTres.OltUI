@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        var uri = new Uri(Navigation.Uri);
        Navigation.NavigateTo($"/Login?ReturnUrl={Uri.EscapeDataString(uri.PathAndQuery)}");
    }
}
